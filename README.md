# Hover Board

A simple hover-effect grid. Move your mouse over the squares to light them up with random colors; they fade back to dark when the cursor leaves.

## Files

- `index.html` — page markup
- `style.css` — layout and styling
- `main.js` — generates the grid and handles hover color effects

## Usage

Open `index.html` in a browser.
